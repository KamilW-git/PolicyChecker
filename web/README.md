# PolicyChecker — Web Application

This directory contains the Next.js application source code.

**Installation and setup instructions are in the [root README](../README.md).**

The app is intended to run via Docker Compose from the repository root:

```bash
cd ..
docker compose up --build
```

Then open http://localhost:3000
